# Atmosphere backdrops

Drop ambient background JPGs or PNGs here (`bg_01.jpg`, `bg_02.jpg`, …).

Local files override the default remote editorial set at runtime. Prefer 9:16, ~1080×1920. The app applies a dark scrim so UI stays readable.
